# Vault

This repository contains the Vault application split into frontend and backend components.

- `frontend/` contains the client application and encryption logic.
- `backend/` contains the FastAPI backend foundation.

See `backend/README.md` for backend setup and usage.
